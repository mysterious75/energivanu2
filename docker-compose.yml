# =============================================================================
# Energivanu — Docker Compose Configuration
# Production deployment with API server and optional monitoring sidecar
# =============================================================================
#
# Quick Start:
#   docker-compose up -d                          # Start API server
#   docker-compose --profile monitoring up -d     # Start with monitoring
#   docker-compose down                           # Stop all services
#
# Health Check:
#   curl http://localhost:8000/health
#
# API Prediction:
#   curl -X POST http://localhost:8000/predict \
#     -H "Content-Type: application/json" \
#     -d '{"power_trace": [100, 120, 115, 130, 125]}'
#
# =============================================================================

services:

  # ---------------------------------------------------------------------------
  # Primary API Server (CPU)
  # Serves the FastAPI REST endpoints for power prediction & BESS optimization
  # ---------------------------------------------------------------------------
  energivanu-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: energivanu-api
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config:ro        # Configuration (read-only)
      - ./models:/app/models:ro        # Model checkpoints (read-only)
      - energivanu-logs:/app/logs      # Persistent log storage
    environment:
      - PYTHONUNBUFFERED=1
      - ENERGIVANU_ENV=production
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "2.0"
        reservations:
          memory: 512M
          cpus: "0.5"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - energivanu-net

  # ---------------------------------------------------------------------------
  # GPU-Accelerated API Server
  # Same API server with NVIDIA GPU passthrough for accelerated inference
  # Activate with: docker-compose --profile gpu up -d
  # Requires: NVIDIA Container Toolkit (nvidia-ctk)
  # ---------------------------------------------------------------------------
  energivanu-api-gpu:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    container_name: energivanu-api-gpu
    profiles:
      - gpu
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config:ro
      - ./models:/app/models:ro
      - energivanu-logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
      - ENERGIVANU_ENV=production
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    restart: unless-stopped
    runtime: nvidia
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "4.0"
        reservations:
          memory: 1G
          cpus: "1.0"
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - energivanu-net

  # ---------------------------------------------------------------------------
  # Optional: Monitoring Sidecar
  # Runs the CLI demo/monitoring mode for system verification
  # Activate with: docker-compose --profile monitoring up -d
  # ---------------------------------------------------------------------------
  energivanu-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: energivanu-monitor
    command: ["python", "-m", "energivanu.cli", "demo"]
    profiles:
      - monitoring
    volumes:
      - ./config:/app/config:ro
      - ./models:/app/models:ro
      - energivanu-logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
      - ENERGIVANU_ENV=production
    restart: unless-stopped
    networks:
      - energivanu-net

# =============================================================================
# Persistent Volumes
# =============================================================================
volumes:
  energivanu-logs:
    driver: local

# =============================================================================
# Networks
# =============================================================================
networks:
  energivanu-net:
    driver: bridge
